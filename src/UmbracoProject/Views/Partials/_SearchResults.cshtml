@using UmbracoProject.App_Code.Models
@model List<GlobalSearchModel>

@if (Model?.Count > 0)
{
    @foreach (var item in Model)
    {
        <li class="searched-item"><a href="@item?.Url" class="data">@item?.NodeName</a></li>
    }
}
else
{
    <li class="searched-item">Ingen registrering fundet!!</li>
}
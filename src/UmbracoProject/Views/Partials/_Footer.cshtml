@* @using UmbracoProject.App_Code.Models
@model FooterModel *@
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<HomeNavigation>

<!----------------------------------------- Footer --------------------->
<footer class="footer">

    <div class="top-footer text-center space_blue_bg">
        <div class="container-xl">
            <h3 class="white">@Model?.HeadingText</h3>
            @if (Model?.TopFooterButtonList?.Count > 0)
            {
                <div class="multiple-buttons justify-content-center">
                    @foreach (var item in Model?.TopFooterButtonList)
                    {
                        var buttonList = item.Content as IconLinkList;
                        <button type="button" class="btn @buttonList?.ButtonTheme" style="border:1px solid #@buttonList?.BorderColor">
                            <img src="@buttonList?.Icon?.MediaUrl()" class="img-fluid" alt="footer-logo">
                            @buttonList?.Link?.Name
                        </button>

                    }
                </div>
            }
        </div>
    </div>

    <div class="bottom-footer dark_blue_bg style2" style="background-color:#@Model?.BackgroundColor">
        <div class="container-xl">
            <div class="footer-detail">
                <div class="footer-address">
                    <div class="footer-logo">
                        <img src="@Model?.FooterLogo?.MediaUrl()" class="img-fluid" alt="footer-logo">
                    </div>
                    <div class="address white" style="color:#@Model?.FooterTextColor">
                        @Html?.Raw(Model?.FooterAddress)
                    </div>
                    @if (Model?.SocialLinkList?.Count > 0)
                    {
                        <div class="social-icon">
                            <ul class="social-item">

                                @foreach (var item in Model?.SocialLinkList)
                                {
                                    var socialLink = item?.Content as FooterSocialLink;

                                    <li class="social-list light_blue_bg" style="background-color:#@socialLink?.BackgroundColor">
                                        <a href="@socialLink?.SocialLink" class="social-link">
                                            <img src="@socialLink?.Icon?.MediaUrl()" class="img-fluid" alt="social-icon">
                                        </a>
                                    </li>
                                }
                            </ul>
                        </div>
                    }
                </div>

                <div class="footer-menu accordion">

                    @if (Model?.AccordionMenuList?.Count > 0)
                    {
                        var count = 1;
                        @foreach (var item in Model?.AccordionMenuList)
                        {
                            var id = $"collapse{count}";
                            var accordion = item?.Content as FooterMenuAccordion;

                            <div class="footer-menu-list white accordion-item" style="color:#@Model?.FooterTextColor">
                                <button class="accordion-button collapsed fw-700" type="button" data-bs-toggle="collapse" data-bs-target="#@id" aria-expanded="true" aria-controls="@id" style="color:#@Model?.FooterTextColor">
                                    @accordion?.HeadText
                                </button>
                                <ul class="accordion-collapse collapse" id="@id" aria-labelledby="@id">
                                    @foreach (var link in accordion?.MenuItems)
                                    {
                                        <li class="mb-3"><a href="@link?.Url" class="white" style="color:#@Model?.FooterTextColor">@link?.Name</a></li>
                                    }
                                </ul>
                            </div>
                            count++;
                        }
                    }
                </div>
                @if (Model?.DeskTopImage != null)
                {
                    <div class="position-img">
                        <img src="@Model?.DeskTopImage?.MediaUrl()" class="img-fluid" alt="img">
                    </div>
                }
                @if (Model?.MobileImage != null)
                {
                    <div class="mobile-visible d-lg-none">
                        <img src="@Model?.MobileImage?.MediaUrl()" class="img-fluid opacity-30" alt="img">
                    </div>
                }
            </div>
            <div class="copyright">
                <span class="small white" style="color:#@Model?.FooterTextColor">@Model?.CopyRightText</span>
                @if (Model?.FooterLinks?.Count() > 0)
                {
                    @foreach (var item in Model?.FooterLinks)
                    {
                        <span class="small"><a href="@item?.Url" class="white" style="color:#@Model?.FooterTextColor">@item?.Name</a></span>
                    }
                }
            </div>
        </div>
    </div>
</footer>